<UserControl x:Class="FurnaceWPF.Views.Controls.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FurnaceWPF.Views.Controls"
             xmlns:validator="clr-namespace:FurnaceWPF.Validators"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style x:Key="ValidatingTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="ToolTip" 
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self}, 
                                            Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#388E3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Margin" Value="0,20,0,0"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding HasUnsavedChanges}" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </DataTrigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD"/>
                    <Setter Property="Foreground" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="10">
        <CheckBox Content="Режим отладки (Debug Mode)" 
                  IsChecked="{Binding IsDebug}"
                  Margin="0,0,0,15"
                  FontSize="20"
                  IsEnabled="{Binding IsAvailable}"/>

        <TextBlock Text="Настройки зон (0-255):" FontWeight="Bold" FontSize="20" Margin="0,0,0,5"/>

        <TextBlock Text="Адрес Зоны 1:" FontSize="20" Margin="0,5,0,0"/>
        <TextBox Name="Zone1TextBox" Style="{StaticResource ValidatingTextBoxStyle}" IsEnabled="{Binding IsAvailable}" FontSize="20" Margin="0,2,0,5">
            <TextBox.Text>
                <Binding Path="ZoneOneAddress" 
                         UpdateSourceTrigger="Explicit"
                         ValidatesOnDataErrors="True"
                         TargetNullValue="0">
                    <Binding.ValidationRules>
                        <validator:ByteRangeValidationRule Min="0" Max="255"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <TextBlock Text="Адрес Зоны 2:" FontSize="20" Margin="0,5,0,0"/>
        <TextBox Name="Zone2TextBox" Style="{StaticResource ValidatingTextBoxStyle}" IsEnabled="{Binding IsAvailable}" FontSize="20" Margin="0,2,0,5">
            <TextBox.Text>
                <Binding Path="ZoneTwoAddress" 
                         UpdateSourceTrigger="Explicit"
                         ValidatesOnDataErrors="True"
                         TargetNullValue="0">
                    <Binding.ValidationRules>
                        <validator:ByteRangeValidationRule Min="0" Max="255"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <TextBlock Text="Адрес Зоны 3:" FontSize="20" Margin="0,5,0,0"/>
        <TextBox Name="Zone3TextBox" Style="{StaticResource ValidatingTextBoxStyle}" IsEnabled="{Binding IsAvailable}" FontSize="20" Margin="0,2,0,5" Cursor="">
            <TextBox.Text>
                <Binding Path="ZoneThreeAddress" 
                         UpdateSourceTrigger="Explicit"
                         ValidatesOnDataErrors="True"
                         TargetNullValue="0">
                    <Binding.ValidationRules>
                        <validator:ByteRangeValidationRule Min="0" Max="255"/>
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <Button Content="Применить" 
                FontSize="24" 
                Margin="0 20 0 0" IsEnabled="{Binding IsAvailable}"
                Click="Apply_Click" MinHeight="42" Cursor="Hand"/>
    </StackPanel>
</UserControl>